"""Web-based UI layer (pywebview + WebView2).

The analysis engines, the Draft/Command/History model and the agent boundary are
untouched: this package only *reads* them and shapes the result into JSON for the
view. Every number on screen still comes from `setagent.analysis.*`.
"""
